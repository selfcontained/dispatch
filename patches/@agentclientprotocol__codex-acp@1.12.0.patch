diff --git a/dist/index.js b/dist/index.js
index c134fd36ed44f4dccb510f8ac7971f0f7e26aba4..06b9851c496ba7b618211198db878699ed900ddd 100755
--- a/dist/index.js
+++ b/dist/index.js
@@ -25094,9 +25094,13 @@ ${event.details}` : event.summary;
         return createContextCompactionStartUpdate(event.item);
       case "subAgentActivity":
         return this.subagents.legacyActivityStarted(event.item);
+      case "userMessage":
+        return typeof event.item.clientId === "string" ? {
+          sessionUpdate: "session_info_update",
+          _meta: { "dispatch/steering": { pickedUp: event.item.clientId } }
+        } : null;
       case "sleep":
       case "functionCallOutput":
-      case "userMessage":
       case "hookPrompt":
       case "reasoning":
       case "enteredReviewMode":
@@ -28851,6 +28855,7 @@ var CodexAcpClient = class {
     }
     return await this.codexClient.runTurn({
       threadId: request.sessionId,
+      ...(typeof request._meta?.["dispatch/steering"]?.id === "string" ? { clientUserMessageId: request._meta["dispatch/steering"].id } : {}),
       input,
       approvalPolicy: agentMode.approvalPolicy,
       approvalsReviewer: agentMode.approvalsReviewer,
@@ -28982,6 +28987,7 @@ var CodexAcpClient = class {
     return await this.codexClient.turnSteer({
       threadId: params.threadId,
       expectedTurnId: params.turnId,
+      ...(typeof params.receiptId === "string" ? { clientUserMessageId: params.receiptId } : {}),
       input: buildPromptItems(params.prompt)
     });
   }
@@ -32353,6 +32359,7 @@ var CodexAcpServer = class {
       },
       authMethods: getCodexAuthMethods(_params.clientCapabilities),
       _meta: {
+        "dispatch/steering": { pickupReceipts: true, promptReceipts: true },
         steering: {
           supported: true
         },
@@ -33387,6 +33394,9 @@ Check ${configPath} and project .codex directories, especially their config.toml
         return { outcome: "injected" };
       }
     }
+    if (params._meta?.steering?.idleBehavior === "promptRequired") {
+      return { outcome: "promptRequired", reason: "noRunningTurn" };
+    }
     return await this.startNewTurnFromSteering(params);
   }
   /**
@@ -33415,7 +33425,8 @@ Check ${configPath} and project .codex directories, especially their config.toml
       await this.runWithProcessCheck(() => this.codexAcpClient.steerTurn({
         threadId: params.sessionId,
         turnId,
-        prompt: params.prompt
+        prompt: params.prompt,
+        receiptId: params._meta?.["dispatch/steering"]?.id
       }));
       return true;
     } catch (err) {
@@ -33525,7 +33536,8 @@ Check ${configPath} and project .codex directories, especially their config.toml
     }
     return {
       sessionId,
-      prompt
+      prompt,
+      _meta: params["_meta"]
     };
   }
   createSessionConfigOptions(sessionState) {
