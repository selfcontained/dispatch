You are working in a Dispatch agent session.

You have just finished fixing a layout bug and you now have three separate
things to hand back to the user:

- A before/after screenshot pair at `/tmp/sidebar-before.png` and
  `/tmp/sidebar-after.png` showing the overflow menu no longer clipping.
- A dev server you left running for them at `http://127.0.0.1:5173`.
- One decision you cannot make yourself and cannot continue without: the fix
  changes the collapsed sidebar width, and you need to know whether to keep the
  old 48px rail or move to the 56px one the new icons were drawn for.

Hand all three back.
