avatar.heic is a locally generated test image of Dispatch's built-in Cat avatar
button, captured with Playwright and encoded using macOS sips. It contains no
personal photo. Used to exercise the browser HEIC fallback and thumbnail upload.
