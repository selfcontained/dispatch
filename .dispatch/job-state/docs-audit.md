---
job: docs-audit
updated_at: 2026-04-27
---

# docs-audit — state handoff

Each run of the docs-audit job reads this file at Phase 0 and overwrites it at Phase 4. It is the primary mechanism for passing context between runs so the work stays focused and doesn't re-inventory the whole repo every night.

## last_audited_sha

`37d7914710f32952aca028989b369383f8231fc8` — HEAD at the start of the 2026-04-27 Reviewers / push-based round-trip run. The PR that lands this file will advance the SHA once merged; the next run should diff from whatever HEAD is when it reads the file.

## next_focus

**Audit `docs/03-api-spec.md` against the API surface that has accumulated since the last spec refresh.** This has been on the backlog through several runs and the spec is now meaningfully behind. Concrete things to verify:

- New release endpoints — `/api/v1/release/update` (POST), `/api/v1/release/assisted/launch` (POST), `/api/v1/release/assisted/phase` (POST), `/api/v1/release/assisted/state` (GET, DELETE) — and the 409 error codes `ASSISTED_UPDATE_REQUIRED` and `ASSISTED_UPDATE_METADATA_INVALID` returned from the first two. All defined in `apps/server/src/server.ts` (search for `release/update` and `release/assisted`).
- Notifications endpoints — `/api/v1/notifications/settings` (GET, POST), `/api/v1/notifications/test` (POST), and the focus tracker endpoint `/api/v1/focus` (POST).
- New agent endpoints — `POST /api/v1/agents/:id/setup/error` (worktree-failure reporting from PR #409), `POST /api/v1/agents/:id/review-agent-type`, and the agents POST body fields `createNewBranch` (CRU-139) and `role: "standard" | "assisted_update"` (migration 0018).
- Round-trip review server endpoints — anything in the spec covering round-trip reviews is necessarily stale, since the loop is now driven through MCP tools (`dispatch_get_recheck_context`, `dispatch_submit_resolution`, `dispatch_cancel_recheck`) plus server-injected terminal prompts. Make sure the spec doesn't still describe a poll-based REST shape that no longer exists.
- The on-demand jobs run endpoint — `POST /api/v1/jobs/run` with body `{ name, directory, wait?, triggerSource? }` (the older spec may pre-date this).

If the spec is small and contained, do the whole thing in one pass. If it's sprawling, scope the run to the release + assisted-update endpoints (most user-impactful right now since the entire one-click update flow is undocumented) and push the rest back into `backlog` with concrete follow-ups.

If `docs/03-api-spec.md` is not present at the expected path, fall through to the next item in `backlog` — `docs/04-agent-lifecycle.md` is the strongest second pick since `AgentRole`, `markSetupFailed`, and the persona review state machine all touch the lifecycle doc.

## backlog

Items noticed during prior passes but left for later runs. Pick the most relevant one when `next_focus` is empty or already done.

- **docs-pane "Reviewers" section — built-in tool list (the Repo Tools page).** The "Built-in tools" enumeration in the Repo Tools section (`docs-pane.tsx:340–376`) is missing two parent-side round-trip tools that are in `AGENT_TOOLS`: `dispatch_submit_resolution` and `dispatch_cancel_recheck`. They _are_ documented inside the Reviewers Round-trip subsection, but the Repo Tools "Built-in tools" list is what users see when they're scanning what's available, and right now it lists `dispatch_get_feedback` + `dispatch_resolve_feedback` but stops short of the two follow-on tools. One-line fix on the next pass that touches the Repo Tools section.
- **docs-pane "Reviewers" section — resolution capture UI surfaces.** PR #374 (`8ad64b9`) added resolution reasons + parent summary rendering in `feedback-panel.tsx` (`ResolutionInfoBlock` at line 735, resolution `summary` at lines 1179 / 1570). The docs don't describe what the human sees in the Feedback panel for resolved items; could be a one-paragraph addition to "Submitting findings" or "Round-trip reviews".
- **docs-pane "Agents" section — Update badge.** The Agents section under "Status indicators" / "Agent details" doesn't mention the blue **Update** badge that `agent-card.tsx:227–235` renders on `isAssistedUpdateAgent` (`agent.role === "assisted_update"`). The 2026-04-26 run added an Updates section that describes the badge, but the Agents section doesn't currently cross-reference any of the badge variants (Job, Update, Attention). Could add a short "Sidebar badges" subsection that enumerates them.
- **docs-pane "Updates" section — server-side error code surface.** The new section mentions the 409 + `ASSISTED_UPDATE_REQUIRED` flow but not `ASSISTED_UPDATE_METADATA_INVALID` (which the server can return from both `/api/v1/release/update` and `/api/v1/release/assisted/launch` when the target release has a malformed `dispatch-update` block — server.ts:2500, 2594). User-impact is low (only happens when a release ships broken metadata) so leaving it out is reasonable, but worth a sentence if the section gets revisited.
- **docs-pane "Updates" section — release-channel deep mechanics.** Current copy says "stable" follows tagged releases and "latest" follows the most recent including pre-releases. The actual implementation is a single setting (`release_channel` in `dispatch_settings`) that drives `getLatestRelease` filtering. No drift today, but if channel semantics evolve (e.g. a "preview" channel) the docs are at risk.
- **README.md feature list — sound cues bullet missing.** README.md:40-ish lists in-app browser notifications but has no bullet for sound cues (added 2026-04-19 via commit 8704a99) or the mobile-terminal tap sounds (PR #404, `818d48e`, 2026-04-24). The 2026-04-24 run noticed this but didn't fix it because the run was scoped to the Agents section. A one-line addition under "Browser UI with:" closes both.
- **README.md MCP tools summary** — may be out of date for the same reasons docs-pane was when the Repo Tools / Reviewers / Jobs sections were last refreshed. Spot-check against `AGENT_TOOLS` / `JOB_TOOLS` / `PERSONA_TOOLS` whenever a future run touches this area.
- **docs-pane "Status Events" section — `metadata` parameter** — `dispatch_event` accepts an optional `metadata` (Record<string, unknown>) param via `apps/server/src/shared/mcp/server.ts` registerTool input schema. Currently undocumented. It's an advanced/optional field; revisit only if a feature starts using metadata in a user-visible way (none does today — `latestEvent` rendering in `agent-card.tsx` doesn't read metadata).
- **docs-pane "Jobs" section — lifecycle handoff files** — the 2026-04-19 Jobs run seeded the Jobs section with the four lifecycle tools (`job_log` / `job_complete` / `job_failed` / `job_needs_input`) and basic run lifecycle, but did not document the `.dispatch/job-state/<job>.md` handoff convention. That's not a server feature — it's a pattern individual jobs adopt — but the docs could mention it under "Run lifecycle" as a recommended way for agents inside a recurring job to pass context between runs. Check `docs-audit.md` and any other `.dispatch/job-state/*.md` files that exist at the time for concrete examples.
- **docs-pane "Jobs" section — `dispatch_rename_session` is now exposed to job agents** — PR #378 (`3940270`, "Expose dispatch_rename_session to job agents") added the tool to `JOB_TOOLS`. The Jobs section may not yet mention that job agents can rename their session. Quick spot-check next time the Jobs section is touched.
- **docs-pane "Jobs" section — `JobNotifyConfig` is data-model-only today** — 2026-04-20 Notifications pass confirmed that `JobNotifyConfig` (`onComplete`/`onError`/`onNeedsInput`) on `JobRecord` is wired through to Slack via `jobService.onRunStateChange` in `apps/server/src/notifications/job-notifier.ts`, but is **not** exposed via any API or UI: `AddJobInput` / `JobConfigUpdate` don't include `notify`, the add-job form has no toggle, and `POST /api/v1/jobs` can't set it. The DB column exists and stays null for every user-created job. Do **not** document this as a Jobs feature yet — revisit when a UI or API is added.
- **docs-pane "Jobs" section — timeouts** — `timeoutMs` and `needsInputTimeoutMs` are configurable per job (defaults 30 min / 24 h in `apps/server/src/jobs/service.ts`). The current copy mentions "subject to a separate needs-input timeout" but does not call out that either value is configurable. A short bullet would close this.
- **docs-pane "Media & Sharing" section** — audited 2026-04-19. Re-check when `registerShareTool` or `isMediaFile` in `apps/server/src/server.ts` gain new supported extensions (the current docs list is hand-maintained), or when the Share-file upload UI changes.
- **docs-pane "Worktrees" — failure surfaces (PR #409 #1160).** Worktree-create failures now mark the agent `stopped` with `last_error` and a blocked latest_event, surfaced via a new `/api/v1/agents/:id/setup/error` endpoint (server.ts) and `AgentManager.markSetupFailed`. The 2026-04-25 Worktrees pass added the new branch-options copy and deps auto-install bullet but did not document the failure UX. If the failure path becomes surprising in practice, add a short subsection to the Worktrees section explaining what users see when worktree setup fails (red status, error in card).
- **`docs/03-api-spec.md`** — see `next_focus` (current cycle's pick).
- **`docs/04-agent-lifecycle.md`** — verify it still matches `AgentStatus` / `SetupPhase` / `ArchivePhase` in `apps/server/src/agents/manager.ts`. Values: status ∈ {creating, running, stopping, stopped, archiving, error, unknown}; setupPhase ∈ {worktree, env, deps, session, null}; archivePhase ∈ {stopping, worktree-check, worktree-cleanup, finalizing, null}. Plus the persona review state machine (reviewing / awaiting_recheck / complete / cancelled) landed in 0017 — the lifecycle doc may or may not cover it today. Also new: `AgentRole` (`standard` | `assisted_update`) in 0018 — the lifecycle doc does not mention agent roles. PR #409 also added a `markSetupFailed` path that produces a `stopped` status with `last_error` after worktree creation fails — worth checking if the lifecycle doc covers the failure transitions. Strong second-pick for next run if the API spec slot is full.
- **`docs/10-operations-runbook.md`** — verify service management commands match current `bin/dispatch-server` / `bin/dispatch-deploy` flags. PR #377 (`c9e06c0`, "use launchctl kickstart -k for macOS deploy restart") changed deploy/restart behavior — runbook may pre-date this. PR #407 (`496a35a`, "launchd wrapper ABI self-heal", 2026-04-24) added auto-recovery logic to `bin/dispatch-launchd-wrapper` and updated the README's nvm prereq — runbook should cross-reference. Also: assisted-update flow (PR #418) is likely not covered, and the runbook should note that releases marked `mode: required` cannot use the one-click path. The 2026-04-26→27 commit window also cut the server runtime over to Bun (PR #414, plus PR #433 / #434) — the runbook's process-management commands and any "Node version required" claims need a fresh look.
- **`release-notes/AUTHORING.md` cross-references.** The file is the canonical guide for `next-assisted-update.json` authoring (referenced by both AGENTS.md and CLAUDE.md after 2026-04-26). It looks well-maintained today, but if the `AssistedUpdateMetadataSchema` in `release-metadata.ts` gains fields (e.g. a new `requiredCheck` name, a new mode), the AUTHORING doc and the in-app Updates docs both need to be updated in lockstep.
- **Bun runtime cutover (PR #414, #433, #434).** The server now runs on Bun. Top-level docs that mention Node specifically (CLAUDE.md, AGENTS.md, README.md, runbook) should be spot-checked for "Node 20+" / "use npx" / "tsx" claims that may now be wrong (the server uses `bun` for `bin/dispatch-server` and `bin/dispatch-launchd-wrapper`). README.md was touched in 4dc8a9d but other surfaces may not be.

## drift_patterns

Observations about where docs tend to go stale. Each run should add new observations and prune ones that no longer apply.

- **User-facing docs drift fastest.** When the app's behavior changes, the in-app docs-pane content is the first thing to notice. It's also the easiest to update in the same PR as the code change (a reviewer can flag stale copy), but in practice it often isn't — so this audit is where it catches up.
- **Settings panes spawn whole new docs sections.** The 2026-04-19 Jobs audit added a `jobs` docs section because the new Jobs settings pane was undocumented in-app. The 2026-04-26 Updates audit did the same for the Updates section in release-manager.tsx, which had a one-paragraph "Assisted update launches a full-access agent..." in the live UI but no docs-pane coverage at all. When a new top-level Settings pane lands, expect to add a corresponding docs-pane section _and_ an entry to `DocsSection` + `SECTIONS`. Cross-check against the `DOCS_SECTION_NAV` list in any README "user-facing docs" sentence — that string drifts every time a section gets added.
- **Form sections evolve from single-checkbox to multi-checkbox.** Watching CRU-139, the "Create git worktree" checkbox grew into a worktree section with a nested "Create a new branch in this worktree" sub-checkbox. This is a recurring pattern: a single-toggle setting accretes a sub-toggle for a previously-implicit behavior. When auditing a "the X checkbox does Y" claim, open the surrounding `<div>` in the dialog component and look for nearby checkboxes — the docs likely describe the original toggle but miss the nested one.
- **Form labels rename without docs updates.** CRU-139 renamed "Create git worktree" → "Create managed git worktree" in the create dialog. The 2026-04-25 audit caught this. When auditing any "click the X button" or "toggle the Y checkbox" claim, grep the literal label string in the relevant component file — labels rename more often than the feature they're attached to, and the docs almost always lag.
- **Review tooling reshapes by deletion as well as addition.** Reviewers used to ping `review_status` with a `status` + `verdict` param; today `review_status` is a pure progress-ping (`message` only) and completion moved to `dispatch_complete_review`. Round 1 originally added five tools on top, then PR #416 refactored the loop from poll-based (`dispatch_await_review` / `dispatch_await_recheck`) to push-based — and crucially _removed_ those poll tools entirely. The 2026-04-27 audit confirmed they no longer exist in `AGENT_TOOLS` / `PERSONA_TOOLS`, and a new persona-side tool `dispatch_get_recheck_context` was added to fetch round-2 context. Lesson: when a review feature lands, don't just check what's _new_ — also grep for the tool names that were in the docs last time and confirm they still exist. Trace from `PERSONA_TOOLS` + parent-side entries in `AGENT_TOOLS` through `apps/server/src/shared/mcp/server.ts` and the review state transitions in `manager.ts` (`reviewing` / `awaiting_recheck` / `complete` / `cancelled`) to keep up.
- **"Sidebar shows X" claims understate what's actually rendered.** 2026-04-22 found the Status Events section said the sidebar shows "the latest event message and a color-coded status indicator" but `agent-card.tsx` actually renders three things: a colored status label, a relative timestamp from `formatRelativeTime`, and the message. When auditing any "sidebar shows" / "card shows" / "page shows" claim, open the actual rendering component (typically in `apps/web/src/components/app/`) and enumerate every span in the JSX — copy tends to mention only the most prominent one. **Corollary added 2026-04-24:** terminal-type agents change which spans render at all (no event indicator, no full-access badge, no feedback panel, no persona launcher). When a new agent type or role lands, recheck every "card shows X" claim against the actual conditional render — `if (isTerminalAgent) return null` patterns scatter through `agent-card.tsx`. **Corollary added 2026-04-26:** sidebar badges have multiplied (Attention / Job / Update). Every new agent role tends to add a badge — `agent-card.tsx:205–235` enumerates them. When auditing the Agents section, grep `<Badge` in agent-card.tsx and make sure every conditional render is mentioned somewhere. **Corollary added 2026-04-27:** persona-agent-row also has its own state-driven UI surfaces — active terminal badge, round badge, feedback/resolved counts, "Reviewing" / "Rechecking" labels, and a `persona-reviewing-row` highlight class while a review is in progress (`persona-agent-row.tsx:307–390`). Reviewers section claims should be cross-checked against this row, not just the parent agent card.
- **Settings defaults flip without the docs noticing.** 2026-04-18 Worktrees run found the docs claimed the default worktree location was `.dispatch/worktrees/` (nested), but `WorktreeLocationSettings` in `apps/web/src/components/app/settings-pane.tsx` actually defaults to `"sibling"`, and so does `manager.ts` (`worktreeLocation = input.worktreeLocation ?? "sibling"`). When auditing a "default" claim in docs, trace it to the component's `useState(...)` initial value AND to the server-side fallback — the two can disagree, and the docs usually follow whichever one was true when the doc was first written.
- **The built-in MCP tool list sprawls without touching the docs.** The 2026-04-18 Repo Tools pass found the docs-pane listed 7 built-in tools; `AGENT_TOOLS` in `apps/server/src/shared/mcp/server.ts` actually has 18 today (including the round-trip tools but minus the deleted `dispatch_await_review`). Each new tool gets added to `AGENT_TOOLS` / `JOB_TOOLS` / `PERSONA_TOOLS` but almost never to `docs-pane.tsx`. Grep for `AGENT_TOOLS = new Set` and diff against the `Built-in tools` list in `docs-pane.tsx` whenever touching this area. The 2026-04-27 run noticed `dispatch_submit_resolution` + `dispatch_cancel_recheck` are still missing from the Repo Tools list (they're on the backlog).
- **The create-agent dialog accretes options silently.** When `create-agent-dialog.tsx` gains a field or a visibility gate (e.g. `createType !== "terminal"` hiding Full Access / Autonomous Review / Create-with-prompt for the terminal type), the "Creating an agent" bullet list needs a matching entry. Grep `create-agent-dialog.tsx` for `<Checkbox`, `<label`, and `createType !==` to catch new form fields and visibility gates quickly. The same pattern applies to the add-job and edit-job forms in `jobs-pane.tsx` — they share branch selection via `branch-select.tsx`, so a change there can flow through to both Agent and Job docs sections.
- **Agent types list accretes silently.** `AGENT_TYPES` in `apps/web/src/lib/agent-types.ts` started as `claude/codex/opencode` and now includes `terminal`. Docs-pane and README both reference the list verbatim. Grep for the literal tuple `"claude", "codex", "opencode"` in docs and cross-check against `AGENT_TYPES`.
- **Agents have two orthogonal axes: type and role.** 2026-04-24 added a third dimension: `AgentRole` (`standard` | `assisted_update`) in `manager.ts` and migration 0018, layered on top of `AgentType` (`claude` / `codex` / `opencode` / `terminal`). The two axes are independent — a CLI-type agent can have either role; terminal agents are always `standard` in practice. Docs that talk about "kinds of agents" should be precise: the user-visible Type (CLI-vs-terminal) is set in the Create dialog, while Role is set by special creation paths (assisted-update is created from the release-manager Updates section, not the Create dialog). When documenting a new agent variant, ask: is this a new `type`, a new `role`, or just a behavior gated on an existing flag? Reviewers add a third related constraint: the persona launcher `reviewerTypes = enabledAgentTypes.filter(isCliAgentType)` rejects terminal-type agents entirely, even though they're a valid agent type elsewhere.
- **Some agents are created outside the Create dialog.** Personas, jobs, and now the assisted-update agent are all spawned through other UI paths — persona-launcher, jobs-pane "Run now", and the release-manager "Assisted update" / gate card. Any "agents are created via..." claim in docs is incomplete unless it covers all four paths.
- **Status indicator colors are theme-driven via CSS vars (`--status-working`, `--status-blocked`, `--status-waiting`, `--status-done`, `--status-idle`).** Don't trust any hard-coded color-name claim in the docs — resolve the actual hue from `apps/web/src/index.css`. 2026-04-18 found the docs had working/done backwards (they'd been copy-pasted from some earlier palette). 2026-04-22 confirmed the current Status Events copy avoids hue claims entirely (says "color-coded"), which is the durable choice.
- **MCP tool sets change by agent type.** The AGENT / JOB / PERSONA tool lists in `apps/server/src/shared/mcp/server.ts` evolve independently. Any doc that enumerates tools for one role is at risk of drifting from the other two. Keep role-specific lists rather than trying to re-list the full set. When documenting a tool, always state which agent types can see it.
- **Some tools are gated dynamically per agent.** `dispatch_get_recheck_context` is added to the persona-side allowed set only when `context.agent?.review?.allowRecheck === true` (`mcp/server.ts:609–614`), and `dispatch_cancel_recheck` is gated on the parent side via the same kind of gate. Docs that imply "persona agents have X tool" may overstate the case for conditionally-allowed tools. When in doubt, grep for `allowed.has(...)` in the registration path and see what conditions hang off it.
- **Repo tool prefix is `repo_`, not `repo.`.** MCP clients don't support dots in tool names, so dots in configured names are sanitized to underscores (`repo-tools.ts`). Docs had `repo.lint` wrong for a long stretch; grep for `repo\.` in docs when reviewing tool-prefix claims.
- **Agent lifecycle has two axes: status and phase.** `status` (`creating`/`running`/…/`archiving`) and `setup_phase` / `archive_phase` are distinct. Docs that mention "phase" without qualifying which one are a drift risk. **Note 2026-04-26:** assisted-update introduced a _third_ set of "phases" (inspect/prepare/apply/restarting/validate/done — see `assisted-update-store.ts`) that are unrelated to the agent setup/archive phases. When documenting "phases" in a future area, name the specific axis.
- **Persona prompt assembly strips legacy placeholders.** `apps/server/src/personas/loader.ts` strips `{{context}}` and `{{diff}}` from persona bodies and appends its own context/diff/feedback-guidance sections. Anywhere the docs describe persona file contents, cross-check against `assemblePersonaPrompt`.
- **Job-related state lives in multiple places.** A "Job" has fields on the DB row (`use_worktree`, `base_branch`, `auto_archive`, `notify`, …), on the `JobRecord` TS type, in the add-job form state (`keepAgent` inverts to `autoArchive`), and in the `buildJobPrompt` preamble that is sent to the agent on every run. When auditing a Jobs claim, trace the field from the UI form → `AddJobConfig` / `JobConfigUpdate` → `JobStore.createJob` / `updateJob` → `JobRecord` → `buildRunConfig` / `buildJobPrompt`. The UI labels (e.g. "Keep agent") do not always match the DB column name (`auto_archive`). **Corollary:** a field can exist on `JobRecord` (e.g. `notify`) without ever being reachable through `AddJobInput` or the form — if you see a feature documented on the record but missing from the input type, assume it's not user-configurable yet.
- **Media formats are gated by two separate allow-lists.** The accepted extensions for `dispatch_share` live in `isMediaFile` / `TEXT_EXTENSIONS` / `DOCUMENT_EXTENSIONS` in `apps/server/src/server.ts`, while the upload-button `accept=` list lives in `ACCEPTED_EXTENSIONS` in `apps/web/src/components/app/media-sidebar.tsx`. They should stay in sync; drift between them creates a confusing UX where an agent can share a format the UI refuses to upload (or vice versa). Diff both when touching either.
- **Notification delivery is multi-channel and they overlap.** Three independent channels (Slack via webhook, web push via SSE `notification` event + ack, sound cues in `use-agent-sound-cues.ts`) all fire off the same `agentManager.onLatestEvent` hook in `apps/server/src/server.ts`. Plus `dispatch_notify` is an agent-initiated fourth path that only hits Slack. Per-channel toggles live in different places — Slack settings + web settings are DB-backed via `slackNotifier`, sound cues are client-side only via `soundCuesEnabledAtom` in `apps/web/src/lib/store.ts`. When documenting "how do I turn X off?", always name which channel.
- **Focus tracker semantics are broader than "terminal open."** `FocusTracker` keys on `agentId` + a 30s TTL, fed by periodic heartbeats from `apps/web/src/hooks/use-agent-focus.ts`. The hook only pulses when `selectedAgentId` is set AND `document.hasFocus()` AND `!document.hidden` — i.e. any tab that has the agent selected, focused, and visible, not specifically the terminal pane. Docs that say "terminal open" or "viewing the terminal" are narrower than reality.
- **Release-flow surface area has metadata-driven UI branches.** The Updates section's Update / Assisted-update / Gate-card branching is driven by `info.assisted` (presence) and `info.assisted.mode` (`normal` / `recommended` / `required`) plus the server-computed `info.assistedRequired` boolean. Docs that describe "the Update button" or "the Assisted update button" need to cover all four states (no metadata, mode=normal, mode=recommended, mode=required), not just the happy path. If a future run revisits this section, walk every JSX branch under `if (info)` in `release-manager.tsx`.
- **CLAUDE.md and AGENTS.md drift from each other.** AGENTS.md got an "Assisted Update Release Notes" section in PR #418 (CRU-143) that CLAUDE.md was missing until the 2026-04-26 audit. The `<!-- Keep behavioral rules in sync with AGENTS.md -->` comment at the top of CLAUDE.md is the canonical pointer; treat any AGENTS.md change as a CLAUDE.md change too. When auditing, run `diff CLAUDE.md AGENTS.md` and look at any block that exists in one but not the other (after accounting for the few legitimately one-sided sections).
- **Push-based prompt injection makes some "what the agent sees" claims wrong.** The 2026-04-27 audit found the Reviewers Round-trip section was claiming that the round-2 prompt to the reviewer "contains" the resolution summary, per-item resolutions, and round-1 diff. In reality the prompt is short and tells the reviewer to call `dispatch_get_recheck_context` to load that data — the agent sees the prompt, then the tool result, not one inlined blob. Whenever `apps/server/src/reviews/injection-prompts.ts` (or any future server-side prompt builder) is edited, cross-check the docs that describe what the receiving agent "sees".

## Notes for the next run

- If `next_focus` feels too big for one night, split it and push half back into `backlog` with a concrete description.
- Treat each run as one focused slice. Recent passes (Agents, Repo Tools, Reviewers, Worktrees, Jobs, Media, Notifications, Status Events, Reviewers round-trip, Agents — terminal type, Worktrees — CRU-139, Updates — assisted-update, Reviewers — push-based) all fit comfortably in one PR each. Aim for that size.
- If you notice something that isn't drift but is genuinely missing (e.g. a new feature with no docs anywhere), add it to `backlog` with enough detail that a future run can act on it without re-discovering the context.
- When the diff since last_audited_sha includes a meaningful UI change (PR #418 / CRU-143 was a good example), it's reasonable to pivot away from `next_focus` to handle the diff-driven update first — the docs-actively-wrong case beats the docs-silent case for impact.

## History

- **2026-04-18** (Agents) — Audited docs-pane "Agents" section. Fixed status-indicator color mapping (working/done were swapped in the docs), added Autonomous Review checkbox and Create-with-prompt flow to the "Creating an agent" bullets, clarified the auto-naming behavior of the Name field.
- **2026-04-18** (Repo Tools) — Audited docs-pane "Repo Tools" section against `repo-tools.ts` and `server.ts`. Fixed the tool prefix from `repo.` → `repo_` (and added a note about dot-to-underscore sanitization), documented the `scope` field for job/reviewer-only tools, expanded the built-in tool list from 7 → 14 entries, and added a one-paragraph note that persona and job agents see different built-in tool subsets.
- **2026-04-18** (Reviewers) — Audited docs-pane "Reviewers" section against `PERSONA_TOOLS` and the review tooling in `apps/server/src/shared/mcp/server.ts` + the loader in `apps/server/src/personas/loader.ts`. Removed the stale `{{context}}`/`{{diff}}` placeholder example (the loader now strips them and auto-appends context/diff/feedback-guidance). Added a "Review lifecycle" subsection covering `review_status` with its `reviewing`/`complete` statuses and `approve`/`request_changes` verdicts. Added a paragraph noting that persona agents also have `dispatch_pin`, `dispatch_share`, and `get_parent_context`. Split "Feedback findings" into "Submitting findings" + "Review lifecycle".
- **2026-04-18** (Worktrees) — Audited docs-pane "Worktrees" section against `apps/server/src/shared/git/worktree.ts`, `apps/server/src/agents/manager.ts` (archive flow + `setupWorktree`), `apps/web/src/components/app/settings-pane.tsx`, and `create-agent-dialog.tsx`. Flipped the reversed "Worktree location" default — code default is `sibling` (`../repo-branch-name`), not `.dispatch/worktrees/`. Automatic-creation, archive-cleanup (auto/keep/force), and parallel-agents claims all matched reality. Deps auto-install omission pushed to backlog.
- **2026-04-19** (Jobs) — Seeded a new docs-pane "Jobs" section between "Repo Tools" and "Worktrees". Covers creating a job (all form fields including Name / Working directory / Prompt / Schedule / Agent type / Full access / Use worktree + base branch / Keep agent after run completes / Enable on schedule), on-demand runs (Run now + `triggerSource: "manual"`), run lifecycle (`job_log` / `job_complete` / `job_failed` / `job_needs_input` plus the seven run statuses), and history/status. Used `Briefcase` icon, added `"jobs"` to the `DocsSection` union. Lifecycle handoff-file convention and `JobNotifyConfig` / configurable timeouts deferred to backlog.
- **2026-04-19** (Media & Sharing) — Audited docs-pane "Media & Sharing" section against `registerShareTool` / `mcpShareMedia` / `isMediaFile` in `apps/server/src/server.ts` and the sidebar UI in `apps/web/src/components/app/media-sidebar.tsx`. Added PDF to the supported formats list and called out the full text-extension family. Split "Sharing media" into three subsections: file-path sharing, text-snippet sharing (`content` + `name`, 32KB cap), and in-place updates via the `update` parameter. Fleshed out Simulator subsection with `simulatorUdid` defaulting to booted. Added a "Listing shared media" subsection for `dispatch_list_media`. Added a paragraph to the Media sidebar section describing the "Share file" upload button (`source: "user"`) and noting the "most recent 50" limit on the list endpoint.
- **2026-04-20** (Notifications) — Audited docs-pane "Notifications" section against `apps/server/src/notifications/slack.ts`, `apps/server/src/focus-tracker.ts`, `apps/web/src/hooks/use-agent-focus.ts`, `apps/web/src/components/app/notification-settings.tsx`, `apps/web/src/lib/sound-cues.ts`, and the `dispatch_notify` registration in `apps/server/src/shared/mcp/server.ts`. Rewrote the section: added Browser Notifications subsection (PWA/iOS caveat, Slack-fallback behavior), Sound Cues subsection (four cues including `review_finished`, per-device client-only state), and Agent-Initiated Notifications subsection covering `dispatch_notify` (title/message/level/respectFocus, 5/min rate limit, available to agent + job types). Fixed "Focus-aware suppression" framing — it's now "tab visible and focused with this agent selected" (30s TTL), not "terminal open" (which was both wrong and narrower than the actual behavior). Updated Slack-test button copy to "Send Slack test". Deferred job-level `JobNotifyConfig` documentation to backlog because it's not user-configurable yet (no form, no API to set it).
- **2026-04-22** (Status Events) — Audited docs-pane "Status Events" section against `AGENT_LATEST_EVENT_TYPES` in `apps/server/src/server.ts`, the `dispatch_event` tool registration in `apps/server/src/shared/mcp/server.ts`, the agent-card rendering in `apps/web/src/components/app/agent-card.tsx`, and the Activity-page surfaces in `apps/web/src/components/app/activity-pane.tsx`. Event-type list (working/blocked/waiting_user/done/idle) and Activity claims (heatmaps, working-time breakdowns, daily status charts) both still match the code. Updated the "How events are used" copy to reflect what the sidebar actually renders — it shows three things, not just the message + indicator: a colored status label (Working / Blocked / Waiting / Done / Idle from `latestEventLabel`), a relative timestamp from `formatRelativeTime` ("just now" / "5m ago" / "2h ago" / "3d ago"), and the message. The optional `metadata` parameter on `dispatch_event` is still undocumented and pushed to backlog (no UI surfaces it yet).
- **2026-04-23** (Reviewers — round-trip) — Audited docs-pane "Reviewers" section against `PERSONA_TOOLS` / `AGENT_TOOLS` in `apps/server/src/shared/mcp/server.ts`, the review state machine in `apps/server/src/agents/manager.ts` (`reviewing` → `complete` → `awaiting_recheck` → `complete` round 2, plus `cancelled`), `apps/server/src/reviews/poll-cadence.ts` (180s → 300s → 600s, 2h timeout), and migration `0017_persona-review-round-trip.sql`. Fixed the "Review lifecycle" copy — `review_status` is now a progress-ping only (takes a `message`, not a `status`), and completion moved to `dispatch_complete_review` (takes verdict / summary / filesReviewed / message). Updated the "How personas work" paragraph to mention `dispatch_complete_review` in the flow. Added a new "Round-trip reviews" subsection covering the parent-side loop (`dispatch_launch_persona allowRecheck: true`, `dispatch_await_review`, `dispatch_get_feedback`, `dispatch_resolve_feedback`, `dispatch_submit_resolution`, `dispatch_cancel_recheck`) and the reviewer-side `dispatch_await_recheck` + second `dispatch_complete_review` call, including the "commit before submitting resolution" warning and the ~2-hour auto-cancel timeout. Pushed launcher UI + resolution-panel UI surfaces to backlog. Next focus: Agents section vs new `terminal` agent type and assisted-update role.
- **2026-04-24** (Agents — terminal type) — Audited docs-pane "Agents" section against `AGENT_TYPES` / `CLI_AGENT_TYPES` in `apps/web/src/lib/agent-types.ts` and the gating logic in `create-agent-dialog.tsx` (`createType !== "terminal"` at lines 492 and 548). Updated the **Type** bullet to list `terminal` alongside the three CLI types and explain it's a plain tmux shell with no CLI assistant. Marked **Full access mode**, **Autonomous Review**, and **Create with prompt** as CLI-only since they're hidden for terminal agents. Updated **Status indicators** to note that terminal-agent cards stay neutral (`agent-card.tsx:326` only renders `latestEvent` when `!isTerminalAgent`). Updated **Agent details** to note that the full-access badge, feedback panel (`agent-card.tsx:535`), and persona launcher (`agent-card.tsx:558`) are also hidden for terminal agents. Also added `terminal` to the README features list (line 26). Assisted-update role explicitly deferred to next run because it spans more UI surfaces (release-manager UpdatesSection) and may warrant a new "Updates" docs section rather than a sub-bullet under Agents.
- **2026-04-25** (Worktrees — CRU-139) — Pivoted from the assisted-update next_focus to a diff-driven Worktrees update because PR #409 (`a5e8d52`, "decouple managed worktree from new branch creation") rendered the docs-pane Worktrees section actively wrong. Renamed the checkbox in the **Automatic worktree creation** subsection from "Create git worktree" to "Create managed git worktree" (matches `create-agent-dialog.tsx`), added the "disabled when cwd isn't a git repo" behavior, and moved the env-copy + lockfile-driven deps auto-install (`pnpm-lock.yaml` / `yarn.lock` / `package-lock.json` / `bun.lockb` via `setupWorktree` in `manager.ts`) into a separate sentence — closing the long-standing "deps auto-install" backlog item from the 2026-04-18 Worktrees pass. Added a new **Branch options** subsection that explains the nested **Create a new branch in this worktree** sub-checkbox: on (default) forks a working branch from the starting branch, off checks out the starting branch directly for review/investigation flows. Added a paragraph to **Cleaning up** noting the new branch-preservation behavior — when `worktreeBranch === baseBranch` (i.e. createNewBranch was off), archive cleanup removes only the worktree directory and leaves the user's branch alone (`agent.worktreePath` cleanup with `deleteBranch: dispatchOwnsBranch` in `manager.ts:1171–1178`). Assisted-update role still deferred — pushed to next_focus again.
- **2026-04-26** (Updates — assisted-update / CRU-143) — Closed the long-standing assisted-update next_focus by adding a new top-level docs-pane **Updates** section using the `ArrowDownToLine` icon. The section walks the entire release-manager UI: current version card, release channel toggle (stable / latest), Check for updates → version-available view, one-click **Update to vX.Y.Z** flow (with the 409 + `ASSISTED_UPDATE_REQUIRED` gate), assisted-update flow (full-access agent on production checkout, blue **Update** badge from `agent-card.tsx:227–235`, single-active-at-a-time guard from `hasActiveAssistedUpdateAgent` in `server.ts:972`), the four metadata modes (no metadata / `normal` / `recommended` / `required`) and how each one renders the buttons + gate card, the agent's structured phase progression (inspect → prepare → apply → restarting → validate → done with `blocked` / `rollback` failure phases) per `apps/server/src/assisted-update-store.ts`, the per-phase notes + structured `requiredChecks` results panel in `assisted-update-card.tsx`, and the Reload / Clear cache & reload dropdown. Cross-referenced `apps/server/src/release-metadata.ts` for the schema (`AssistedUpdateMetadataSchema`, `appliesFrom` semver gate via `isAssistedUpdateRequired`), `apps/server/src/server.ts:2451–2534` for the `/api/v1/release/update` 409 path, and `apps/server/src/server.ts:2536–2690` for the `/api/v1/release/assisted/launch` path. Also synced **CLAUDE.md** with AGENTS.md by adding the matching **Assisted Update Release Notes** authoring block (the `<!-- Keep behavioral rules in sync -->` comment at the top of CLAUDE.md was being violated since PR #418). Updated the README's "user-facing documentation" sentence to include `jobs` (already in docs-pane since 2026-04-19 but missing from the README index) and `updates` (new). Pivoted next_focus to Reviewers vs PR #416's push-based round-trip refactor — the current Round-trip subsection still describes poll-based tools that may no longer exist.
- **2026-04-27** (Reviewers — push-based) — Closed the next_focus from 2026-04-26. Audited docs-pane "Reviewers" section against `AGENT_TOOLS` / `PERSONA_TOOLS` in `apps/server/src/shared/mcp/server.ts` and the new `apps/server/src/reviews/injection-prompts.ts`. Confirmed the previously suspected deletions: `dispatch_await_review` and `dispatch_await_recheck` no longer exist as registered tools. The Reviewers section already opened with "The handoff is push-based... There is no tool to poll." which was correct after PR #416, so the parent-side tool list (`dispatch_get_feedback` / `dispatch_resolve_feedback` / `dispatch_submit_resolution` / `dispatch_cancel_recheck`) needed no edits. Two real corrections: (1) the round-2 paragraph claimed the injected reviewer prompt "contains" the resolution summary, per-item resolutions, and round-1 diff — actually the prompt is short and tells the reviewer to call `dispatch_get_recheck_context` (a new persona-side tool gated on `review.allowRecheck`), which returns those three things plus a commit range to `git diff` locally. Updated the paragraph and added `respondsToFeedbackId` for round-2 findings; (2) added a one-line note in "How personas work" that reviewers always run as a CLI-type agent because `reviewerTypes = enabledAgentTypes.filter(isCliAgentType)` in `persona-launcher.tsx`. Also rolled the persona-agent-row `persona-reviewing-row` highlight into the closing UI sentence. Two long-standing backlog items closed (launcher UI surfaces + 2026-04-23 push-based round-trip cleanup). Repo Tools "Built-in tools" list still missing `dispatch_submit_resolution` + `dispatch_cancel_recheck`; left on backlog because it's not in the Reviewers slice and a one-line fix is better folded into the next Repo Tools touch. Pivoted next_focus to `docs/03-api-spec.md` — long-deferred and the release/assisted-update/notifications endpoints have been undocumented for at least two cycles.
