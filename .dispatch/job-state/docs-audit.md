---
job: docs-audit
updated_at: 2026-05-02
---

# docs-audit — state handoff

Each run of the docs-audit job reads this file at Phase 0 and overwrites it at Phase 4. It is the primary mechanism for passing context between runs so the work stays focused and doesn't re-inventory the whole repo every night.

## last_audited_sha

`fa25db28ba0631ac4a6d223e449e8d021366ae31` — HEAD at the start of the 2026-05-02 docs-pane Personalities run. The PR that lands this file will advance the SHA once merged; the next run should diff from whatever HEAD is when it reads the file.

## next_focus

**Audit the docs-pane "Agents" section against the new diff-stats badge + tmux scrollback assist.** Two user-facing surfaces landed since the 2026-05-01 audit and the Agents section understates both:

- **Diff-stats badge on the expanded agent card.** PR #477 (`fa25db2`, 2026-05-02) added a `+X/-Y` badge in the top-right of the agent-card detail panel (`apps/web/src/components/app/diff-stat-badge.tsx`, rendered from `agent-card.tsx:402`). The badge is hidden when the agent has no diff (added=0 and deleted=0), flashes when stats change, dims when stale (latest event newer than `computedAt` + 30 s), and is clickable to force a refresh. Backed by `GET /api/v1/agents/:id/diff-stats` in `apps/server/src/routes/agents.ts:790` plus the `diffStatsRefresher` background loop. The Agents section's "Agent details" paragraph (`docs-pane.tsx`) currently lists "working directory or worktree path, git branch, and agent type" — add the diff badge to that enumeration and write a short subsection describing the badge UI (flash, stale dimming, tap-to-refresh, hidden when no changes). Per `drift_patterns."Sidebar shows X" claims understate what's actually rendered`.
- **Tmux scrollback assist setting.** A new `CopyModeAssistSettings` block lives in Settings → Agents (`apps/web/src/components/app/settings-pane.tsx:786`) with copy "Detect tmux copy mode, show the scrollback banner, and add a return-to-live shortcut. When off, Dispatch does not change tmux mouse mode or observe copy mode at all." Off by default. Drives the `TerminalCopyModeBanner` (`apps/web/src/components/app/terminal-copy-mode-banner.tsx`) and the server-side `copy-mode-observer.ts` + `copy-mode-assist-manager.ts` plumbing. Persisted via `copy_mode_assist_enabled` setting + `POST /api/v1/agents/settings`. The Agents docs section says nothing about scrollback, copy mode, or how to get back to live — add a short "Tmux scrollback" subsection covering the setting, the banner that appears when the user enters copy mode, and the return-to-live action.

If both fit comfortably in one PR, do them together. If not, split: badge first (smaller, closes the active gap in the "Agent details" paragraph), scrollback assist second (larger, requires checking what the banner actually says/looks like).

## backlog

Items noticed during prior passes but left for later runs. Pick the most relevant one when `next_focus` is empty or already done.

- **docs-pane "Agents" — diff-stats badge + scrollback assist.** See `next_focus`.
- **docs-pane "Media & Sharing" — pin / drawer mode (PR #468).** PR #468 (`Default media sidebar to drawer mode with optional pin`, 2026-04-29) made the media sidebar default to a drawer with a Pin/PinOff toggle (icons in `media-sidebar.tsx`). The Media & Sharing section was last touched 2026-04-19 and predates both the per-agent state change (PR #461) and this default-mode + pin change. One-paragraph addition under whatever the section currently calls "opening the sidebar."
- **README.md feature list — sound cues + personalities bullets missing.** README.md lists in-app browser notifications but has no bullet for sound cues (added 2026-04-19 via commit 8704a99) or the mobile-terminal tap sounds (PR #404, 2026-04-24); also missing a bullet for personalities (PR #462, now documented in-app as of 2026-05-02). PR #436 ("Refresh README copy and screenshot", 2026-04-26) did not add these — re-checked on 2026-04-28. Three one-line additions under "Browser UI with:" close all three.
- **README.md "user-facing docs" section list.** When a future README pass touches the in-app docs sentence, cross-check the section list against `DOCS_SECTION_NAV` in `docs-pane.tsx`. The list now includes `personalities` (added 2026-05-02) on top of the prior nine sections — README is at risk of lagging.
- **README.md MCP tools summary** — may be out of date for the same reasons docs-pane was when the Repo Tools / Reviewers / Jobs sections were last refreshed. Spot-check against `AGENT_TOOLS` / `JOB_TOOLS` / `PERSONA_TOOLS` whenever a future run touches this area. JOB_TOOLS gained 6 entries in PR #466 and was reflected in docs-pane (2026-05-01); README probably still understates the surface.
- **docs/03-api-spec.md — `/api/v1/personalities` endpoints missing.** The full CRUD surface (`GET /personalities`, `POST /personalities`, `PATCH /personalities/:id`, `DELETE /personalities/:id`, `POST /personalities/active`) plus the body shapes (`{ name, prompt }`, `name ≤ 80`, `prompt ≤ 1000`, 409 on duplicate name) are undocumented. Add when an api-spec pass next runs. Also the agents-settings endpoint (`POST /api/v1/agents/settings` for `copyModeAssistEnabled`) and `GET /api/v1/agents/:id/diff-stats` are new — same omission.
- **docs/03-api-spec.md remaining gaps.** The 2026-04-28 pass covered the missing endpoints and error codes but did not refresh: (1) the `Notifications` section's claim that the Slack-fallback delay is "~3s" — verify against `apps/server/src/notifications/slack.ts`; (2) the Streaming section's CDP details — confirm the multipart MJPEG framing claim is still accurate; (3) the Media POST description: `{ file, description }` — `description` may now be optional or accept a `name` field too. Quick spot-checks for a future API run.
- **docs-pane "Reviewers" section — resolution capture UI surfaces.** PR #374 (`8ad64b9`) added resolution reasons + parent summary rendering in `feedback-panel.tsx` (`ResolutionInfoBlock` at line 735, resolution `summary` at lines 1179 / 1570). The docs don't describe what the human sees in the Feedback panel for resolved items; could be a one-paragraph addition to "Submitting findings" or "Round-trip reviews".
- **docs-pane "Agents" section — sidebar badges enumeration.** The Agents section under "Status indicators" / "Agent details" doesn't enumerate every conditional badge that `agent-card.tsx:205–235` renders (Job, Update, Attention, full-access). The Updates section already describes the Update badge; a single "Sidebar badges" subsection in Agents that enumerates them all would close the cross-reference gap. The new diff-stats badge (`next_focus`) is a separate surface and lives in the expanded card, not the row header.
- **docs-pane "Updates" section — `ASSISTED_UPDATE_METADATA_INVALID`.** The Updates section mentions the 409 + `ASSISTED_UPDATE_REQUIRED` flow but not `ASSISTED_UPDATE_METADATA_INVALID` (also returned by `/api/v1/release/assisted/launch` per the 2026-04-28 audit). User-impact is low (only happens when a release ships broken metadata) so leaving it out is reasonable, but worth a sentence if the section gets revisited. Also still uncovered: `MIGRATION_EVALUATION_UNAVAILABLE` (503) — the 2026-04-30 run mentioned the `migrationsError` warning in user copy but did not name the underlying error code.
- **docs-pane "Updates" section — release-channel deep mechanics.** Current copy says "stable" follows tagged releases and "latest" follows the most recent including pre-releases. The actual implementation is a single setting (`release_channel` in `dispatch_settings`) that drives `getLatestRelease` filtering. No drift today, but if channel semantics evolve (e.g. a "preview" channel) the docs are at risk.
- **docs-pane "Status Events" section — `metadata` parameter** — `dispatch_event` accepts an optional `metadata` (Record<string, unknown>) param via `apps/server/src/shared/mcp/server.ts` registerTool input schema. Currently undocumented in docs-pane but documented in the API spec as of 2026-04-28. Advanced/optional field; revisit only if a feature starts using metadata in a user-visible way.
- **docs-pane "Jobs" section — `JobNotifyConfig` is data-model-only today** — 2026-04-20 Notifications pass confirmed that `JobNotifyConfig` (`onComplete`/`onError`/`onNeedsInput`) on `JobRecord` is wired through to Slack via `jobService.onRunStateChange` in `apps/server/src/notifications/job-notifier.ts`, but is not exposed via any API or UI. Do not document this as a Jobs feature yet — revisit when a UI or API is added.
- **docs-pane "Media & Sharing" section** — audited 2026-04-19. Re-check when `registerShareTool` or `isMediaFile` in `apps/server/src/server.ts` gain new supported extensions, or when the Share-file upload UI changes. Also: PR #461 (2026-04-28) made media-sidebar state per-agent — worth a one-sentence note in the Media section if it gets revisited. PR #468 (2026-04-29) is now the more recent driver — see the dedicated backlog item above.
- **docs-pane "Worktrees" — failure surfaces (PR #409).** Worktree-create failures now mark the agent `stopped` with `last_error` and a blocked latest_event, surfaced via `/api/v1/agents/:id/setup/error` (in the API spec as of 2026-04-28 and lifecycle doc as of 2026-04-29). The docs-pane Worktrees section did not get the failure UX paragraph — if the failure path becomes surprising in practice, add a short subsection to Worktrees explaining what users see when worktree setup fails.
- **`docs/04-agent-lifecycle.md` — round-trip back from in-app docs.** The 2026-04-29 rewrite (PR #456) covered roles / setup phases / archive phases / assisted-update phase axis / persona review state machine. If the in-app docs-pane gains a "Lifecycle" or "Architecture" section in the future, link to this doc rather than re-explaining the state machine.
- **`release-notes/AUTHORING.md` — migration manifest authoring is undocumented.** AUTHORING.md is the canonical guide for `next-assisted-update.json` (referenced by AGENTS.md and CLAUDE.md). CRU-146 added `update-migrations/*.yaml` manifests with their own evaluator (`update-migrations-evaluator.ts`), and the runbook now describes them, but AUTHORING.md does not yet cover them. If a future run picks this area, add a "Migration manifests" subsection to AUTHORING.md or split into a sibling `update-migrations/AUTHORING.md`.
- **`docs/10-operations-runbook.md` — handed-off concerns.** The 2026-04-29 ops-runbook rewrite removed the wrong "auto-rollback" / "polls /api/v1/health" claims, removed the wrong "writes last-release-failure.log" claim (server doesn't write it — only the assisted-update agent prompt mentions it), and added the assisted-update phase axis + required-check names. Open items if a future run revisits: (a) the diagnostics jq examples are still accurate today (shape unchanged) but worth a re-grep against `apps/server/src/routes/system.ts:554` if the response shape is ever rev'd; (b) the runbook does not mention `bin/preflight` / `bin/dispatch-stream` / `bin/pack-release` — they exist and operators may need them; consider adding a "Release-time tooling" subsection if operators ask for it.
- **`unknown` AgentStatus is dead code.** The 2026-04-29 lifecycle rewrite documented `unknown` as "reserved/transitional". Worth a separate cleanup PR (not docs work) to either delete it from the type or actually wire a transition that sets it. The defensive query in `release-runtime.ts:197` (`status IN ('creating', 'running', 'stopping', 'unknown')`) is the only consumer.

## drift_patterns

Observations about where docs tend to go stale. Each run should add new observations and prune ones that no longer apply.

- **Operator-facing prose docs accumulate aspirational claims.** The 2026-04-29 ops-runbook audit found three independent claims describing behavior the code never had (auto-rollback, post-update health polling, diagnosis-agent on workflow failure) plus one referenced-but-never-written file (`last-release-failure.log` — only mentioned in the assisted-update prompt template, never opened by the server). Lesson: when auditing operator-facing docs, every "the server does X" claim is suspect. Grep the source for the verb (`spawn`, `writeFile`, `wait`, `poll`, `rollback`) and confirm the call exists. Don't assume docs that match a plausible mental model match the code.
- **API-spec drift compounds quietly.** The 2026-04-28 audit found `docs/03-api-spec.md` had drifted on six independent surfaces without any single PR being responsible — each landed a route or field, the spec wasn't touched, the gap accumulated. When the audit pivots to api-spec, expect to reconcile against `grep -nE '(server|app)\.(get|post|patch|delete)' apps/server/src/server.ts` rather than diff-driven discovery — the diffs since last audit only catch the most recent one or two endpoints. New 2026-05-02 confirmation: the personalities CRUD surface (5 routes) plus `/api/v1/agents/settings` and `/api/v1/agents/:id/diff-stats` all landed without an api-spec touch — pattern still active.
- **Lifecycle/runbook doc drift is the same shape as API-spec drift.** Dev/ops-facing prose docs grow many small independent gaps without any single PR breaking them. Expect them to need full re-grounding against the source of truth (`apps/server/src/agents/manager.ts`, `assisted-update-store.ts`, `apps/server/src/server/release-runtime.ts`) rather than diff-driven patching when they get touched.
- **User-facing docs drift fastest.** When the app's behavior changes, the in-app docs-pane content is the first thing to notice. It's also the easiest to update in the same PR as the code change (a reviewer can flag stale copy), but in practice it often isn't — so this audit is where it catches up.
- **Settings panes spawn whole new docs sections.** The 2026-04-19 Jobs audit added a `jobs` docs section because the new Jobs settings pane was undocumented. The 2026-04-26 Updates audit did the same for the Updates section. The 2026-05-02 Personalities audit did the same — Personalities was added as a sub-block of Settings → Agents (not its own top-level tab) but justified its own docs section because it's a distinct user-managed feature with a non-obvious scope ("not used for review personas or jobs"). Pattern refinement: a new feature deserves its own docs section when the surface area is large enough OR when its name overlaps with another concept (Personalities vs. Reviewer "personas" — the disambiguation alone earns a section heading).
- **Form sections evolve from single-checkbox to multi-checkbox.** Watching CRU-139, the "Create git worktree" checkbox grew into a worktree section with a nested "Create a new branch in this worktree" sub-checkbox. Recurring pattern: a single-toggle setting accretes a sub-toggle for a previously-implicit behavior. When auditing a "the X checkbox does Y" claim, open the surrounding `<div>` in the dialog component and look for nearby checkboxes — the docs likely describe the original toggle but miss the nested one.
- **Form labels rename without docs updates.** CRU-139 renamed "Create git worktree" → "Create managed git worktree". When auditing any "click the X button" or "toggle the Y checkbox" claim, grep the literal label string in the relevant component file — labels rename more often than the feature they're attached to, and the docs almost always lag. New 2026-05-01 example: the Jobs add form's "Enable on schedule" surface is labeled `Enabled` (a switch) and only appears once a schedule is set — the previous docs copy described it as if it were always present.
- **Review tooling reshapes by deletion as well as addition.** Reviewers used to ping `review_status` with a `status` + `verdict` param; today `review_status` is a pure progress-ping (`message` only) and completion moved to `dispatch_complete_review`. PR #416 refactored the loop from poll-based to push-based and removed `dispatch_await_review` / `dispatch_await_recheck` entirely. When a review feature lands, don't just check what's new — also grep for the tool names that were in the docs last time and confirm they still exist.
- **"Sidebar shows X" claims understate what's actually rendered.** When auditing any "sidebar shows" / "card shows" / "page shows" claim, open the actual rendering component and enumerate every span in the JSX — copy tends to mention only the most prominent one. Terminal-type agents change which spans render at all (no event indicator, no full-access badge, no feedback panel, no persona launcher); persona-agent-row has its own state-driven UI surfaces; sidebar badges have multiplied. Every new agent role tends to add a badge — `agent-card.tsx:205–235` enumerates them. New 2026-05-02 confirmation: PR #477 added a diff-stats badge inside the expanded agent card (`agent-card.tsx:402`) — Agent details copy still understates this surface; queued in `next_focus`.
- **Settings defaults flip without the docs noticing.** When auditing a "default" claim in docs, trace it to the component's `useState(...)` initial value AND to the server-side fallback — the two can disagree, and the docs usually follow whichever one was true when the doc was first written.
- **The built-in MCP tool list sprawls without touching the docs.** Each new tool gets added to `AGENT_TOOLS` / `JOB_TOOLS` / `PERSONA_TOOLS` but almost never to `docs-pane.tsx`. Grep for `AGENT_TOOLS = new Set` and diff against the `Built-in tools` list in `docs-pane.tsx` whenever touching this area. PR #466 added six tools to JOB_TOOLS (`dispatch_list_media`, `dispatch_launch_persona`, `dispatch_get_feedback`, `dispatch_resolve_feedback`, `dispatch_submit_resolution`, `dispatch_cancel_recheck`); the 2026-05-01 Jobs run reconciled docs-pane to the current set and added the two missing entries (`dispatch_submit_resolution`, `dispatch_cancel_recheck`) to the Repo Tools "Built-in tools" list. Expect this to recur — the lists evolve often.
- **The create-agent dialog accretes options silently.** When `create-agent-dialog.tsx` gains a field or a visibility gate, the "Creating an agent" bullet list needs a matching entry. The same pattern applies to the add-job and edit-job forms in `jobs-pane.tsx`.
- **Form bullets diverge from the form's own visual hierarchy.** New 2026-05-01 observation: the Jobs add form has a basics section, a dedicated Prompt card, and an "Advanced settings" disclosure (timeouts / worktree / full access / keep agent). The previous docs flattened all of these into a single bullet list, which made advanced fields read as if they were always visible. When a form has a disclosure, mirror that hierarchy in the bullets — users scan docs against what they see on screen.
- **Agent types list accretes silently.** `AGENT_TYPES` in `apps/web/src/lib/agent-types.ts` started as `claude/codex/opencode` and now includes `terminal`. Docs-pane and README both reference the list verbatim. Server has its own copy at `apps/server/src/agent-type-settings.ts:5` — they must stay in sync. Note: jobs use `JobAgentType` (`claude/codex/opencode`) — terminal agents can't run jobs, so the Jobs section's enumeration intentionally diverges from the global list. Personalities (added 2026-05-02) intentionally diverge too — terminal agents have no CLI to inject system-prompt text into, so the personality is silently skipped.
- **Agents have two orthogonal axes: type and role.** `AgentRole` (`standard` | `assisted_update`) is independent of `AgentType` (`claude` / `codex` / `opencode` / `terminal`). The user-visible Type is set in the Create dialog; Role is set by special creation paths (assisted-update is created from the release-manager Updates section, not the Create dialog). When documenting a new agent variant, ask: is this a new `type`, a new `role`, or just a behavior gated on an existing flag?
- **Some agents are created outside the Create dialog.** Personas, jobs, and the assisted-update agent are all spawned through other UI paths — persona-launcher, jobs-pane "Run now", and the release-manager. The 2026-04-28 audit confirmed there are also two server-side review-launch endpoints (`/agents/:id/launch-review` and `/agents/:id/persona-reviews`) that create persona agents without going through the dialog at all. Any "agents are created via..." claim in docs is incomplete unless it covers all paths.
- **Status indicator colors are theme-driven via CSS vars (`--status-working`, `--status-blocked`, `--status-waiting`, `--status-done`, `--status-idle`).** Don't trust any hard-coded color-name claim in the docs — resolve the actual hue from `apps/web/src/index.css`.
- **MCP tool sets change by agent type.** The AGENT / JOB / PERSONA tool lists in `apps/server/src/shared/mcp/server.ts` evolve independently. Any doc that enumerates tools for one role is at risk of drifting from the other two. Keep role-specific lists rather than trying to re-list the full set. When documenting a tool, always state which agent types can see it.
- **Some tools are gated dynamically per agent.** `dispatch_get_recheck_context` is added to the persona-side allowed set only when `context.agent?.review?.allowRecheck === true`, and `dispatch_cancel_recheck` is gated on the parent side via the same kind of gate. Docs that imply "persona agents have X tool" may overstate the case for conditionally-allowed tools.
- **System-prompt injection has a per-flow allowlist.** New 2026-05-02 observation: Personalities are appended to the system prompt only for "regular" launches — explicitly NOT for `input.persona`, `input.jobRunId`, or `role === "assisted_update"` (`apps/server/src/agents/manager.ts:494`). When documenting any future "all agents see X" injection (CLAUDE.md content, MCP server URL, augmented prompts), check for this same skip-list pattern and don't overstate the scope. The injection sites in `command-builder.ts` differ per CLI: Claude uses `--append-system-prompt`, OpenCode/Codex prepend to the launch prompt. Terminal agents have no CLI and silently skip.
- **Repo tool prefix is `repo_`, not `repo.`.** MCP clients don't support dots in tool names, so dots in configured names are sanitized to underscores (`repo-tools.ts`).
- **Agent lifecycle has multiple "phase" axes.** `setup_phase` (worktree/env/deps/session), `archive_phase` (stopping/worktree-check/worktree-cleanup/finalizing), and the assisted-update phase (`inspect`/`prepare`/`apply`/`restarting`/`validate`/`done` + `blocked`/`rollback`/`failed` from `assisted-update-store.ts`) are three independent state machines. Docs that mention "phase" without naming the axis are a drift risk. The 2026-04-29 lifecycle doc and runbook both spell out the assisted-update axis explicitly.
- **Persona prompt assembly strips legacy placeholders.** `apps/server/src/personas/loader.ts` strips `{{context}}` and `{{diff}}` from persona bodies and appends its own context/diff/feedback-guidance sections.
- **Job-related state lives in multiple places.** A "Job" has fields on the DB row, on the `JobRecord` TS type, in the add-job form state (`keepAgent` inverts to `autoArchive`), and in the `buildJobPrompt` preamble that is sent to the agent on every run. The UI labels (e.g. "Keep agent") do not always match the DB column name (`auto_archive`). A field can exist on `JobRecord` (e.g. `notify`) without ever being reachable through `AddJobInput` or the form — if you see a feature documented on the record but missing from the input type, assume it's not user-configurable yet.
- **Media formats are gated by two separate allow-lists.** The accepted extensions for `dispatch_share` live in `isMediaFile` / `TEXT_EXTENSIONS` / `DOCUMENT_EXTENSIONS` in `apps/server/src/server.ts`, while the upload-button `accept=` list lives in `ACCEPTED_EXTENSIONS` in `apps/web/src/components/app/media-sidebar.tsx`. They should stay in sync.
- **Notification delivery is multi-channel and they overlap.** Three independent channels (Slack via webhook, web push via SSE `notification` event + ack, sound cues in `use-agent-sound-cues.ts`) all fire off the same `agentManager.onLatestEvent` hook. Plus `dispatch_notify` is an agent-initiated fourth path that only hits Slack. When documenting "how do I turn X off?", always name which channel.
- **Focus tracker semantics are broader than "terminal open."** `FocusTracker` keys on `agentId` + a 30s TTL, fed by periodic heartbeats from `apps/web/src/hooks/use-agent-focus.ts`. The hook only pulses when `selectedAgentId` is set AND `document.hasFocus()` AND `!document.hidden`. Docs that say "terminal open" or "viewing the terminal" are narrower than reality.
- **Release-flow surface area has metadata-driven UI branches.** The Updates section's Update / Assisted-update / Gate-card branching is driven by `info.assisted` (presence) and `info.assisted.mode` (`normal` / `recommended` / `required`) plus the server-computed `info.assistedRequired` boolean. CRU-146 added a fourth driver — pending update migrations — which can independently force the assisted path even if no `dispatch-update` block exists. Docs that describe "the Update button" need to cover all permutations.
- **Gated UI hides escape hatches in dropdown menu items, not primary buttons.** New observation 2026-04-30: the Updates `UpdateActions` split button surfaces the override path as a _menu item_ (not a hidden / disabled state). When auditing any "this is unavailable" or "this button is hidden" claim, open the surrounding split-button / dropdown component and check the menu items — gated UIs in this codebase consistently keep the action reachable behind a confirmation dialog rather than removing it. Pattern shows up wherever the server returns 409 + a force-override; expect more instances as gates accrete.
- **Per-install state files live outside the repo checkout.** `~/.dispatch/release.json` (deployed tag), `~/.dispatch/assisted-update.json` (in-flight assisted state), `~/.dispatch/applied-migrations.json` (CRU-146), `~/.dispatch/cache/release-<tag>.tar.gz` (artifact cache). When auditing operator-facing docs, the temptation is to look only at the server checkout (`~/.dispatch/server/`) — but the source-of-truth state for "what's deployed" and "what's in flight" is one level up.
- **CLAUDE.md and AGENTS.md drift from each other.** AGENTS.md got an "Assisted Update Release Notes" section in PR #418 (CRU-143) that CLAUDE.md was missing until the 2026-04-26 audit. The `<!-- Keep behavioral rules in sync with AGENTS.md -->` comment at the top of CLAUDE.md is the canonical pointer; treat any AGENTS.md change as a CLAUDE.md change too.
- **Push-based prompt injection makes some "what the agent sees" claims wrong.** Whenever `apps/server/src/reviews/injection-prompts.ts` (or any future server-side prompt builder) is edited, cross-check the docs that describe what the receiving agent "sees".
- **Type-vs-actually-set drift.** The 2026-04-29 lifecycle audit found `unknown` is in the `AgentStatus` TypeScript union but never assigned by any code path. Defensive queries in `release-runtime.ts` include it in the `status IN (...)` list. When documenting types, cross-check that values are actually written somewhere — a value that only appears in a type alias and a defensive query is dead-code-ish and worth either removing or wiring up.

## Notes for the next run

- If `next_focus` feels too big for one night, split it and push half back into `backlog` with a concrete description.
- Treat each run as one focused slice. Recent passes (Agents, Repo Tools, Reviewers, Worktrees, Jobs, Media, Notifications, Status Events, Reviewers round-trip, Updates, Reviewers push-based, API spec, Agent lifecycle, Ops runbook, Updates v2, Jobs v2, Personalities) all fit comfortably in one PR each. Aim for that size.
- If you notice something that isn't drift but is genuinely missing (e.g. a new feature with no docs anywhere), add it to `backlog` with enough detail that a future run can act on it without re-discovering the context.
- When the diff since `last_audited_sha` includes a meaningful UI change, it's reasonable to pivot away from `next_focus` to handle the diff-driven update first — the docs-actively-wrong case beats the docs-silent case for impact.

## History

- **2026-04-18** (Agents) — Audited docs-pane "Agents" section. Fixed status-indicator color mapping, added Autonomous Review checkbox and Create-with-prompt flow, clarified auto-naming.
- **2026-04-18** (Repo Tools) — Audited docs-pane "Repo Tools" section. Fixed tool prefix `repo.` → `repo_`, documented `scope`, expanded built-in tool list 7 → 14.
- **2026-04-18** (Reviewers) — Audited docs-pane "Reviewers" section. Removed stale `{{context}}`/`{{diff}}` placeholders, added Review lifecycle subsection.
- **2026-04-18** (Worktrees) — Audited docs-pane "Worktrees" section. Flipped reversed worktree-location default (sibling, not nested).
- **2026-04-19** (Jobs) — Seeded a new docs-pane "Jobs" section (form fields, run lifecycle, on-demand runs, history).
- **2026-04-19** (Media & Sharing) — Audited docs-pane "Media & Sharing" section. Added PDF + text-extension family, split sharing into three subsections.
- **2026-04-20** (Notifications) — Audited docs-pane "Notifications" section. Rewrote into Browser Notifications / Sound Cues / Agent-Initiated subsections; fixed focus-aware suppression framing.
- **2026-04-22** (Status Events) — Audited docs-pane "Status Events" section. Updated "How events are used" copy to reflect three rendered spans.
- **2026-04-23** (Reviewers — round-trip) — Audited docs-pane "Reviewers" section. Fixed `review_status` (progress-ping only), added Round-trip subsection covering parent + reviewer-side tools.
- **2026-04-24** (Agents — terminal type) — Audited docs-pane "Agents" section. Added terminal type, marked Full access / Autonomous Review / Create-with-prompt as CLI-only, noted hidden surfaces for terminal agents.
- **2026-04-25** (Worktrees — CRU-139) — Pivoted to Worktrees because PR #409 renamed checkbox to "Create managed git worktree". Added Branch options subsection (createNewBranch sub-checkbox), branch-preservation behavior on archive, and lockfile-driven deps auto-install.
- **2026-04-26** (Updates — assisted-update / CRU-143) — Added new top-level docs-pane Updates section (release channel, one-click update, assisted update, gate-card, three metadata modes, structured phase progression). Synced CLAUDE.md with AGENTS.md (Assisted Update Release Notes block).
- **2026-04-27** (Reviewers — push-based) — Audited docs-pane "Reviewers" section against PR #416's push-based refactor. Confirmed `dispatch_await_review` / `dispatch_await_recheck` are gone; rewrote round-2 paragraph to reflect `dispatch_get_recheck_context`; added one-line note that reviewers run as CLI-type agents only.
- **2026-04-28** (API spec) — Closed the long-deferred `docs/03-api-spec.md` next_focus. Added `role` to the Agent Model JSON; updated `POST /agents` body; added `/agents/:id/setup/error`, `/agents/:id/launch-review`, and `/agents/:id/persona-reviews`; updated Feedback PATCH; added the four `release/assisted/*` endpoints with body shapes; documented 409 error codes and bearer-token bypass; added `503 MIGRATION_EVALUATION_UNAVAILABLE`; documented `POST /jobs/run` body; added `terminal` to the agent-types Settings row. Pivoted next_focus to `docs/04-agent-lifecycle.md`.
- **2026-04-29** (Agent lifecycle) — Closed the long-deferred `docs/04-agent-lifecycle.md` next_focus. Rewrote against `apps/server/src/agents/manager.ts`. Added Roles / Setup Phases / Archive Phases / `markSetupFailed → stopped` / full reconciliation table / force-stop semantics / corrected `?cleanupWorktree=auto|keep|force` / phase-axes table / Assisted-Update Phase Axis section / Persona Review State Machine. Pivoted next_focus to `docs/10-operations-runbook.md`.
- **2026-04-29** (Ops runbook — Bun + assisted-update + CRU-146) — Closed the long-deferred ops-runbook next_focus. Added per-install state-files list to Architecture Overview. Documented the compiled-Bun-binary runtime and `bin/dispatch-launchd-wrapper` selection logic. Replaced Service Management with the `bin/dispatch-server` wrapper (preferred path) plus modern `launchctl bootstrap`/`bootout`/`kickstart -k` examples. Removed wrong "spawns a diagnosis agent" claim from Release Pipeline; corrected workflow steps to match `pnpm run ci` + Bun build. Rewrote Update To A Tag: documented the assisted-update / migration gate (409 `ASSISTED_UPDATE_REQUIRED`, 503 `MIGRATION_EVALUATION_UNAVAILABLE`, `force=true` override), the artifact-first deploy path with `dispatch-release.tar.gz` cache, runtime-binary verification, and the kickstart restart. Removed wrong "polls /api/v1/health" / "writes last-release-failure.log" / "auto-rollback" claims. Added a new Assisted Update section covering `/release/assisted/launch`, the `inspect → … → done` phase axis with terminal `rollback / blocked / failed`, the five `requiredCheck` names, and the `applied-migrations.json` write on success. Updated CI Pipeline to match `.github/workflows/ci.yml`. Updated Installation to reflect artifact-first deploy + `release.json` seeding. Expanded File Locations table with state files, cache, dispatch.log rotation note, and wrapper-script pointer. Pivoted next_focus to docs-pane "Updates" section against the post-CRU-146 reality.
- **2026-04-30** (Updates v2 — pending migrations / split-button / failed phase) — Closed the docs-pane Updates next_focus. Rewrote One-click and Assisted-update sections against `apps/web/src/components/app/release-manager.tsx` + `assisted-update-card.tsx`. Renamed the section heading "Assisted update" → "Agent-assisted update" (matches the actual button label). Replaced the incorrect "the one-click button is hidden" claim with the unified split-button + force-override-dialog reality. Documented three independent drivers of the assisted path (pending migrations / release-body metadata / evaluator failure), the `pendingMigrations` and `migrationsError` fields surfaced on `/release/info`, and the split-button mode-vs-state matrix. Added terminal `failed` to the assisted-update phase enumeration alongside `blocked` / `rollback`. Pivoted next_focus to docs-pane "Jobs" section against the JOB_TOOLS expansion in PR #466.
- **2026-05-01** (Jobs v2 — JOB_TOOLS expansion / handoff convention / form hierarchy) — Closed the docs-pane Jobs next_focus. Refactored the "Creating a job" bullet list into a basic / Advanced split that mirrors the form's "Advanced settings" disclosure; added `Run timeout, minutes` + `Wait for input, minutes` bullets with defaults (30 min / 24 h); fixed the schedule label ("Cron schedule") and the conditional `Enabled` switch; clarified that terminal-type agents can't run jobs and that Dispatch prepends a job-prompt preamble with run ID. Added new subsections: "Tools available to job agents" (groups the JOB_TOOLS surface into status/comms, PRs, discovery, round-trip review), "Auto-review with personas" (notes that jobs can run an end-to-end self-review loop and points at the Reviewers section), and "State across runs" (documents the `.dispatch/job-state/<job>.md` handoff convention). Also closed the Repo Tools "Built-in tools" backlog item by adding `dispatch_submit_resolution` and `dispatch_cancel_recheck` to the standard-agent enumeration. Pivoted next_focus to docs-pane "Personalities" section against PR #462.
- **2026-05-02** (Personalities) — Closed the docs-pane Personalities next_focus. Added a new top-level docs-pane section (`personalities`) between Agents and Repo Tools, with `Sparkles` icon. Three subsections: "Managing personalities" (Settings → Agents location, Name ≤ 80 / Prompt ≤ 1000, Edit/Delete actions, edits don't affect already-launched agents); "Activating" (single-active radio toggle, no per-agent override); "What it applies to" (Claude `--append-system-prompt` flag vs. Codex/OpenCode prepend; intentionally skipped for persona reviewers, job runs, and `assisted_update` agents; silently skipped for terminal agents). Disambiguated from review "personas" in the lead. Verified against `personality-settings.tsx`, `routes/personalities.ts`, `db/personalities.ts`, and the injection sites in `agents/manager.ts:494`/`680` + `agents/tmux/command-builder.ts:330`. Added `personalities` to `DocsSection` union and `DOCS_SECTION_NAV`. Pivoted next_focus to docs-pane "Agents" section against the diff-stats badge (PR #477) and tmux scrollback assist (`CopyModeAssistSettings` + `TerminalCopyModeBanner`).
